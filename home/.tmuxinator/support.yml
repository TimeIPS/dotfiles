# ~/.tmuxinator/support.yml
# you can make as many tabs as you wish...

project_name: Remote-Service
project_root: /root/
tabs:
  - Shell:
  - Logs:
      layout: main-vertical
      panes:
        - tail -F /var/log/tips/php.log
        - tail -F /var/log/tips/ajax.log
        - top
